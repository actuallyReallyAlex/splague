import { LOL_BUTT } from '../actions/actions'

const game = (state = [], action) => {
  switch (action.type) {
    case LOL_BUTT:
      return state
    default:
      return state
  }
}

export default game